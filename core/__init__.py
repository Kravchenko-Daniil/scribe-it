"""core — единственное место продуктовой логики scribe-it.

resolve (источник → MediaItem'ы) · download (yt-dlp / ffmpeg→opus / yandex) ·
scribe (ElevenLabs Scribe + рендер) · storage (workdir'ы) · cache (Scribe-кеш).
"""
